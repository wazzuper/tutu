<h1><%= t('.title') %></h1>

<table>
  <thead>
    <tr>
      <th>Номер поезда</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <%= render @trains %>
  </tbody>
</table>

<br>

<%= link_to t('common.main_menu'), admin_welcome_index_path %>
<%= link_to t('.add_train'), new_admin_train_path %>